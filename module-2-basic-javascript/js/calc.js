// create an function for additions
function ad() {
  var a = parseInt(prompt('Enter a values :'));
  var b = parseInt(prompt('Enter b values :'));
  var res = a + b;
  alert('Additions of numbers is :' + res);
}

// create an function for substractions
function subs() {
  var a = parseInt(prompt('Enter a values :'));
  var b = parseInt(prompt('Enter b values :'));
  var res = a - b;
  alert('Substractions of numbers is :' + res);
}

// create an function for multiplications
function mult() {
  var a = parseInt(prompt('Enter a values :'));
  var b = parseInt(prompt('Enter b values :'));
  var res = a * b;
  alert('Multiplications of numbers is :' + res);
}

// create an function for divsions
function dv() {
  var a = parseInt(prompt('Enter a values :'));
  var b = parseInt(prompt('Enter b values :'));
  var res = a / b;
  alert('Divisions of numbers is :' + res);
}
